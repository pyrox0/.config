;;; config/config.el -*- lexical-binding: t; -*-
;;(after! org-load-hook
;;  load (concat hedge-config-dir "config/org") nil 'nomessage)
;; (after! minimap-mode-hook
;;  load (concat hedge-config-dir "config/minimap") nil 'nomessage)
